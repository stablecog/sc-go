// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/stablecog/sc-go/database/ent/usernameblacklist"
)

// UsernameBlacklist is the model entity for the UsernameBlacklist schema.
type UsernameBlacklist struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// Username holds the value of the "username" field.
	Username string `json:"username,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt    time.Time `json:"updated_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*UsernameBlacklist) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case usernameblacklist.FieldUsername:
			values[i] = new(sql.NullString)
		case usernameblacklist.FieldCreatedAt, usernameblacklist.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case usernameblacklist.FieldID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the UsernameBlacklist fields.
func (ub *UsernameBlacklist) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case usernameblacklist.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				ub.ID = *value
			}
		case usernameblacklist.FieldUsername:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field username", values[i])
			} else if value.Valid {
				ub.Username = value.String
			}
		case usernameblacklist.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				ub.CreatedAt = value.Time
			}
		case usernameblacklist.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				ub.UpdatedAt = value.Time
			}
		default:
			ub.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the UsernameBlacklist.
// This includes values selected through modifiers, order, etc.
func (ub *UsernameBlacklist) Value(name string) (ent.Value, error) {
	return ub.selectValues.Get(name)
}

// Update returns a builder for updating this UsernameBlacklist.
// Note that you need to call UsernameBlacklist.Unwrap() before calling this method if this UsernameBlacklist
// was returned from a transaction, and the transaction was committed or rolled back.
func (ub *UsernameBlacklist) Update() *UsernameBlacklistUpdateOne {
	return NewUsernameBlacklistClient(ub.config).UpdateOne(ub)
}

// Unwrap unwraps the UsernameBlacklist entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (ub *UsernameBlacklist) Unwrap() *UsernameBlacklist {
	_tx, ok := ub.config.driver.(*txDriver)
	if !ok {
		panic("ent: UsernameBlacklist is not a transactional entity")
	}
	ub.config.driver = _tx.drv
	return ub
}

// String implements the fmt.Stringer.
func (ub *UsernameBlacklist) String() string {
	var builder strings.Builder
	builder.WriteString("UsernameBlacklist(")
	builder.WriteString(fmt.Sprintf("id=%v, ", ub.ID))
	builder.WriteString("username=")
	builder.WriteString(ub.Username)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(ub.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(ub.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// UsernameBlacklists is a parsable slice of UsernameBlacklist.
type UsernameBlacklists []*UsernameBlacklist
