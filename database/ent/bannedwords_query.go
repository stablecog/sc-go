// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"github.com/stablecog/sc-go/database/ent/bannedwords"
	"github.com/stablecog/sc-go/database/ent/predicate"
)

// BannedWordsQuery is the builder for querying BannedWords entities.
type BannedWordsQuery struct {
	config
	ctx        *QueryContext
	order      []OrderFunc
	inters     []Interceptor
	predicates []predicate.BannedWords
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the BannedWordsQuery builder.
func (bwq *BannedWordsQuery) Where(ps ...predicate.BannedWords) *BannedWordsQuery {
	bwq.predicates = append(bwq.predicates, ps...)
	return bwq
}

// Limit the number of records to be returned by this query.
func (bwq *BannedWordsQuery) Limit(limit int) *BannedWordsQuery {
	bwq.ctx.Limit = &limit
	return bwq
}

// Offset to start from.
func (bwq *BannedWordsQuery) Offset(offset int) *BannedWordsQuery {
	bwq.ctx.Offset = &offset
	return bwq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (bwq *BannedWordsQuery) Unique(unique bool) *BannedWordsQuery {
	bwq.ctx.Unique = &unique
	return bwq
}

// Order specifies how the records should be ordered.
func (bwq *BannedWordsQuery) Order(o ...OrderFunc) *BannedWordsQuery {
	bwq.order = append(bwq.order, o...)
	return bwq
}

// First returns the first BannedWords entity from the query.
// Returns a *NotFoundError when no BannedWords was found.
func (bwq *BannedWordsQuery) First(ctx context.Context) (*BannedWords, error) {
	nodes, err := bwq.Limit(1).All(setContextOp(ctx, bwq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{bannedwords.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (bwq *BannedWordsQuery) FirstX(ctx context.Context) *BannedWords {
	node, err := bwq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first BannedWords ID from the query.
// Returns a *NotFoundError when no BannedWords ID was found.
func (bwq *BannedWordsQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = bwq.Limit(1).IDs(setContextOp(ctx, bwq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{bannedwords.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (bwq *BannedWordsQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := bwq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single BannedWords entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one BannedWords entity is found.
// Returns a *NotFoundError when no BannedWords entities are found.
func (bwq *BannedWordsQuery) Only(ctx context.Context) (*BannedWords, error) {
	nodes, err := bwq.Limit(2).All(setContextOp(ctx, bwq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{bannedwords.Label}
	default:
		return nil, &NotSingularError{bannedwords.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (bwq *BannedWordsQuery) OnlyX(ctx context.Context) *BannedWords {
	node, err := bwq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only BannedWords ID in the query.
// Returns a *NotSingularError when more than one BannedWords ID is found.
// Returns a *NotFoundError when no entities are found.
func (bwq *BannedWordsQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = bwq.Limit(2).IDs(setContextOp(ctx, bwq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{bannedwords.Label}
	default:
		err = &NotSingularError{bannedwords.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (bwq *BannedWordsQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := bwq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of BannedWordsSlice.
func (bwq *BannedWordsQuery) All(ctx context.Context) ([]*BannedWords, error) {
	ctx = setContextOp(ctx, bwq.ctx, "All")
	if err := bwq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*BannedWords, *BannedWordsQuery]()
	return withInterceptors[[]*BannedWords](ctx, bwq, qr, bwq.inters)
}

// AllX is like All, but panics if an error occurs.
func (bwq *BannedWordsQuery) AllX(ctx context.Context) []*BannedWords {
	nodes, err := bwq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of BannedWords IDs.
func (bwq *BannedWordsQuery) IDs(ctx context.Context) ([]uuid.UUID, error) {
	var ids []uuid.UUID
	ctx = setContextOp(ctx, bwq.ctx, "IDs")
	if err := bwq.Select(bannedwords.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (bwq *BannedWordsQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := bwq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (bwq *BannedWordsQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, bwq.ctx, "Count")
	if err := bwq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, bwq, querierCount[*BannedWordsQuery](), bwq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (bwq *BannedWordsQuery) CountX(ctx context.Context) int {
	count, err := bwq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (bwq *BannedWordsQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, bwq.ctx, "Exist")
	switch _, err := bwq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (bwq *BannedWordsQuery) ExistX(ctx context.Context) bool {
	exist, err := bwq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the BannedWordsQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (bwq *BannedWordsQuery) Clone() *BannedWordsQuery {
	if bwq == nil {
		return nil
	}
	return &BannedWordsQuery{
		config:     bwq.config,
		ctx:        bwq.ctx.Clone(),
		order:      append([]OrderFunc{}, bwq.order...),
		inters:     append([]Interceptor{}, bwq.inters...),
		predicates: append([]predicate.BannedWords{}, bwq.predicates...),
		// clone intermediate query.
		sql:  bwq.sql.Clone(),
		path: bwq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Words []string `json:"words,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.BannedWords.Query().
//		GroupBy(bannedwords.FieldWords).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (bwq *BannedWordsQuery) GroupBy(field string, fields ...string) *BannedWordsGroupBy {
	bwq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &BannedWordsGroupBy{build: bwq}
	grbuild.flds = &bwq.ctx.Fields
	grbuild.label = bannedwords.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Words []string `json:"words,omitempty"`
//	}
//
//	client.BannedWords.Query().
//		Select(bannedwords.FieldWords).
//		Scan(ctx, &v)
func (bwq *BannedWordsQuery) Select(fields ...string) *BannedWordsSelect {
	bwq.ctx.Fields = append(bwq.ctx.Fields, fields...)
	sbuild := &BannedWordsSelect{BannedWordsQuery: bwq}
	sbuild.label = bannedwords.Label
	sbuild.flds, sbuild.scan = &bwq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a BannedWordsSelect configured with the given aggregations.
func (bwq *BannedWordsQuery) Aggregate(fns ...AggregateFunc) *BannedWordsSelect {
	return bwq.Select().Aggregate(fns...)
}

func (bwq *BannedWordsQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range bwq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, bwq); err != nil {
				return err
			}
		}
	}
	for _, f := range bwq.ctx.Fields {
		if !bannedwords.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if bwq.path != nil {
		prev, err := bwq.path(ctx)
		if err != nil {
			return err
		}
		bwq.sql = prev
	}
	return nil
}

func (bwq *BannedWordsQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*BannedWords, error) {
	var (
		nodes = []*BannedWords{}
		_spec = bwq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*BannedWords).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &BannedWords{config: bwq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(bwq.modifiers) > 0 {
		_spec.Modifiers = bwq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, bwq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (bwq *BannedWordsQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := bwq.querySpec()
	if len(bwq.modifiers) > 0 {
		_spec.Modifiers = bwq.modifiers
	}
	_spec.Node.Columns = bwq.ctx.Fields
	if len(bwq.ctx.Fields) > 0 {
		_spec.Unique = bwq.ctx.Unique != nil && *bwq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, bwq.driver, _spec)
}

func (bwq *BannedWordsQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   bannedwords.Table,
			Columns: bannedwords.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeUUID,
				Column: bannedwords.FieldID,
			},
		},
		From:   bwq.sql,
		Unique: true,
	}
	if unique := bwq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := bwq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, bannedwords.FieldID)
		for i := range fields {
			if fields[i] != bannedwords.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := bwq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := bwq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := bwq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := bwq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (bwq *BannedWordsQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(bwq.driver.Dialect())
	t1 := builder.Table(bannedwords.Table)
	columns := bwq.ctx.Fields
	if len(columns) == 0 {
		columns = bannedwords.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if bwq.sql != nil {
		selector = bwq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if bwq.ctx.Unique != nil && *bwq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range bwq.modifiers {
		m(selector)
	}
	for _, p := range bwq.predicates {
		p(selector)
	}
	for _, p := range bwq.order {
		p(selector)
	}
	if offset := bwq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := bwq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (bwq *BannedWordsQuery) Modify(modifiers ...func(s *sql.Selector)) *BannedWordsSelect {
	bwq.modifiers = append(bwq.modifiers, modifiers...)
	return bwq.Select()
}

// BannedWordsGroupBy is the group-by builder for BannedWords entities.
type BannedWordsGroupBy struct {
	selector
	build *BannedWordsQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (bwgb *BannedWordsGroupBy) Aggregate(fns ...AggregateFunc) *BannedWordsGroupBy {
	bwgb.fns = append(bwgb.fns, fns...)
	return bwgb
}

// Scan applies the selector query and scans the result into the given value.
func (bwgb *BannedWordsGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, bwgb.build.ctx, "GroupBy")
	if err := bwgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*BannedWordsQuery, *BannedWordsGroupBy](ctx, bwgb.build, bwgb, bwgb.build.inters, v)
}

func (bwgb *BannedWordsGroupBy) sqlScan(ctx context.Context, root *BannedWordsQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(bwgb.fns))
	for _, fn := range bwgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*bwgb.flds)+len(bwgb.fns))
		for _, f := range *bwgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*bwgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := bwgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// BannedWordsSelect is the builder for selecting fields of BannedWords entities.
type BannedWordsSelect struct {
	*BannedWordsQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (bws *BannedWordsSelect) Aggregate(fns ...AggregateFunc) *BannedWordsSelect {
	bws.fns = append(bws.fns, fns...)
	return bws
}

// Scan applies the selector query and scans the result into the given value.
func (bws *BannedWordsSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, bws.ctx, "Select")
	if err := bws.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*BannedWordsQuery, *BannedWordsSelect](ctx, bws.BannedWordsQuery, bws, bws.inters, v)
}

func (bws *BannedWordsSelect) sqlScan(ctx context.Context, root *BannedWordsQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(bws.fns))
	for _, fn := range bws.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*bws.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := bws.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (bws *BannedWordsSelect) Modify(modifiers ...func(s *sql.Selector)) *BannedWordsSelect {
	bws.modifiers = append(bws.modifiers, modifiers...)
	return bws
}
