// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"github.com/stablecog/sc-go/database/ent/ipblacklist"
	"github.com/stablecog/sc-go/database/ent/predicate"
)

// IPBlackListQuery is the builder for querying IPBlackList entities.
type IPBlackListQuery struct {
	config
	ctx        *QueryContext
	order      []OrderFunc
	inters     []Interceptor
	predicates []predicate.IPBlackList
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the IPBlackListQuery builder.
func (iblq *IPBlackListQuery) Where(ps ...predicate.IPBlackList) *IPBlackListQuery {
	iblq.predicates = append(iblq.predicates, ps...)
	return iblq
}

// Limit the number of records to be returned by this query.
func (iblq *IPBlackListQuery) Limit(limit int) *IPBlackListQuery {
	iblq.ctx.Limit = &limit
	return iblq
}

// Offset to start from.
func (iblq *IPBlackListQuery) Offset(offset int) *IPBlackListQuery {
	iblq.ctx.Offset = &offset
	return iblq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (iblq *IPBlackListQuery) Unique(unique bool) *IPBlackListQuery {
	iblq.ctx.Unique = &unique
	return iblq
}

// Order specifies how the records should be ordered.
func (iblq *IPBlackListQuery) Order(o ...OrderFunc) *IPBlackListQuery {
	iblq.order = append(iblq.order, o...)
	return iblq
}

// First returns the first IPBlackList entity from the query.
// Returns a *NotFoundError when no IPBlackList was found.
func (iblq *IPBlackListQuery) First(ctx context.Context) (*IPBlackList, error) {
	nodes, err := iblq.Limit(1).All(setContextOp(ctx, iblq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{ipblacklist.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (iblq *IPBlackListQuery) FirstX(ctx context.Context) *IPBlackList {
	node, err := iblq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first IPBlackList ID from the query.
// Returns a *NotFoundError when no IPBlackList ID was found.
func (iblq *IPBlackListQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = iblq.Limit(1).IDs(setContextOp(ctx, iblq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{ipblacklist.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (iblq *IPBlackListQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := iblq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single IPBlackList entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one IPBlackList entity is found.
// Returns a *NotFoundError when no IPBlackList entities are found.
func (iblq *IPBlackListQuery) Only(ctx context.Context) (*IPBlackList, error) {
	nodes, err := iblq.Limit(2).All(setContextOp(ctx, iblq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{ipblacklist.Label}
	default:
		return nil, &NotSingularError{ipblacklist.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (iblq *IPBlackListQuery) OnlyX(ctx context.Context) *IPBlackList {
	node, err := iblq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only IPBlackList ID in the query.
// Returns a *NotSingularError when more than one IPBlackList ID is found.
// Returns a *NotFoundError when no entities are found.
func (iblq *IPBlackListQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = iblq.Limit(2).IDs(setContextOp(ctx, iblq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{ipblacklist.Label}
	default:
		err = &NotSingularError{ipblacklist.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (iblq *IPBlackListQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := iblq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of IPBlackLists.
func (iblq *IPBlackListQuery) All(ctx context.Context) ([]*IPBlackList, error) {
	ctx = setContextOp(ctx, iblq.ctx, "All")
	if err := iblq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*IPBlackList, *IPBlackListQuery]()
	return withInterceptors[[]*IPBlackList](ctx, iblq, qr, iblq.inters)
}

// AllX is like All, but panics if an error occurs.
func (iblq *IPBlackListQuery) AllX(ctx context.Context) []*IPBlackList {
	nodes, err := iblq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of IPBlackList IDs.
func (iblq *IPBlackListQuery) IDs(ctx context.Context) ([]uuid.UUID, error) {
	var ids []uuid.UUID
	ctx = setContextOp(ctx, iblq.ctx, "IDs")
	if err := iblq.Select(ipblacklist.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (iblq *IPBlackListQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := iblq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (iblq *IPBlackListQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, iblq.ctx, "Count")
	if err := iblq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, iblq, querierCount[*IPBlackListQuery](), iblq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (iblq *IPBlackListQuery) CountX(ctx context.Context) int {
	count, err := iblq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (iblq *IPBlackListQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, iblq.ctx, "Exist")
	switch _, err := iblq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (iblq *IPBlackListQuery) ExistX(ctx context.Context) bool {
	exist, err := iblq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the IPBlackListQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (iblq *IPBlackListQuery) Clone() *IPBlackListQuery {
	if iblq == nil {
		return nil
	}
	return &IPBlackListQuery{
		config:     iblq.config,
		ctx:        iblq.ctx.Clone(),
		order:      append([]OrderFunc{}, iblq.order...),
		inters:     append([]Interceptor{}, iblq.inters...),
		predicates: append([]predicate.IPBlackList{}, iblq.predicates...),
		// clone intermediate query.
		sql:  iblq.sql.Clone(),
		path: iblq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		IP string `json:"ip,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.IPBlackList.Query().
//		GroupBy(ipblacklist.FieldIP).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (iblq *IPBlackListQuery) GroupBy(field string, fields ...string) *IPBlackListGroupBy {
	iblq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &IPBlackListGroupBy{build: iblq}
	grbuild.flds = &iblq.ctx.Fields
	grbuild.label = ipblacklist.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		IP string `json:"ip,omitempty"`
//	}
//
//	client.IPBlackList.Query().
//		Select(ipblacklist.FieldIP).
//		Scan(ctx, &v)
func (iblq *IPBlackListQuery) Select(fields ...string) *IPBlackListSelect {
	iblq.ctx.Fields = append(iblq.ctx.Fields, fields...)
	sbuild := &IPBlackListSelect{IPBlackListQuery: iblq}
	sbuild.label = ipblacklist.Label
	sbuild.flds, sbuild.scan = &iblq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a IPBlackListSelect configured with the given aggregations.
func (iblq *IPBlackListQuery) Aggregate(fns ...AggregateFunc) *IPBlackListSelect {
	return iblq.Select().Aggregate(fns...)
}

func (iblq *IPBlackListQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range iblq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, iblq); err != nil {
				return err
			}
		}
	}
	for _, f := range iblq.ctx.Fields {
		if !ipblacklist.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if iblq.path != nil {
		prev, err := iblq.path(ctx)
		if err != nil {
			return err
		}
		iblq.sql = prev
	}
	return nil
}

func (iblq *IPBlackListQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*IPBlackList, error) {
	var (
		nodes = []*IPBlackList{}
		_spec = iblq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*IPBlackList).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &IPBlackList{config: iblq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(iblq.modifiers) > 0 {
		_spec.Modifiers = iblq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, iblq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (iblq *IPBlackListQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := iblq.querySpec()
	if len(iblq.modifiers) > 0 {
		_spec.Modifiers = iblq.modifiers
	}
	_spec.Node.Columns = iblq.ctx.Fields
	if len(iblq.ctx.Fields) > 0 {
		_spec.Unique = iblq.ctx.Unique != nil && *iblq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, iblq.driver, _spec)
}

func (iblq *IPBlackListQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   ipblacklist.Table,
			Columns: ipblacklist.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeUUID,
				Column: ipblacklist.FieldID,
			},
		},
		From:   iblq.sql,
		Unique: true,
	}
	if unique := iblq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := iblq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, ipblacklist.FieldID)
		for i := range fields {
			if fields[i] != ipblacklist.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := iblq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := iblq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := iblq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := iblq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (iblq *IPBlackListQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(iblq.driver.Dialect())
	t1 := builder.Table(ipblacklist.Table)
	columns := iblq.ctx.Fields
	if len(columns) == 0 {
		columns = ipblacklist.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if iblq.sql != nil {
		selector = iblq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if iblq.ctx.Unique != nil && *iblq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range iblq.modifiers {
		m(selector)
	}
	for _, p := range iblq.predicates {
		p(selector)
	}
	for _, p := range iblq.order {
		p(selector)
	}
	if offset := iblq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := iblq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (iblq *IPBlackListQuery) Modify(modifiers ...func(s *sql.Selector)) *IPBlackListSelect {
	iblq.modifiers = append(iblq.modifiers, modifiers...)
	return iblq.Select()
}

// IPBlackListGroupBy is the group-by builder for IPBlackList entities.
type IPBlackListGroupBy struct {
	selector
	build *IPBlackListQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (iblgb *IPBlackListGroupBy) Aggregate(fns ...AggregateFunc) *IPBlackListGroupBy {
	iblgb.fns = append(iblgb.fns, fns...)
	return iblgb
}

// Scan applies the selector query and scans the result into the given value.
func (iblgb *IPBlackListGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, iblgb.build.ctx, "GroupBy")
	if err := iblgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*IPBlackListQuery, *IPBlackListGroupBy](ctx, iblgb.build, iblgb, iblgb.build.inters, v)
}

func (iblgb *IPBlackListGroupBy) sqlScan(ctx context.Context, root *IPBlackListQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(iblgb.fns))
	for _, fn := range iblgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*iblgb.flds)+len(iblgb.fns))
		for _, f := range *iblgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*iblgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := iblgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// IPBlackListSelect is the builder for selecting fields of IPBlackList entities.
type IPBlackListSelect struct {
	*IPBlackListQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ibls *IPBlackListSelect) Aggregate(fns ...AggregateFunc) *IPBlackListSelect {
	ibls.fns = append(ibls.fns, fns...)
	return ibls
}

// Scan applies the selector query and scans the result into the given value.
func (ibls *IPBlackListSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ibls.ctx, "Select")
	if err := ibls.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*IPBlackListQuery, *IPBlackListSelect](ctx, ibls.IPBlackListQuery, ibls, ibls.inters, v)
}

func (ibls *IPBlackListSelect) sqlScan(ctx context.Context, root *IPBlackListQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ibls.fns))
	for _, fn := range ibls.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ibls.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ibls.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (ibls *IPBlackListSelect) Modify(modifiers ...func(s *sql.Selector)) *IPBlackListSelect {
	ibls.modifiers = append(ibls.modifiers, modifiers...)
	return ibls
}
