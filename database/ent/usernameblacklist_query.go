// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"github.com/stablecog/sc-go/database/ent/predicate"
	"github.com/stablecog/sc-go/database/ent/usernameblacklist"
)

// UsernameBlacklistQuery is the builder for querying UsernameBlacklist entities.
type UsernameBlacklistQuery struct {
	config
	ctx        *QueryContext
	order      []usernameblacklist.OrderOption
	inters     []Interceptor
	predicates []predicate.UsernameBlacklist
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the UsernameBlacklistQuery builder.
func (ubq *UsernameBlacklistQuery) Where(ps ...predicate.UsernameBlacklist) *UsernameBlacklistQuery {
	ubq.predicates = append(ubq.predicates, ps...)
	return ubq
}

// Limit the number of records to be returned by this query.
func (ubq *UsernameBlacklistQuery) Limit(limit int) *UsernameBlacklistQuery {
	ubq.ctx.Limit = &limit
	return ubq
}

// Offset to start from.
func (ubq *UsernameBlacklistQuery) Offset(offset int) *UsernameBlacklistQuery {
	ubq.ctx.Offset = &offset
	return ubq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (ubq *UsernameBlacklistQuery) Unique(unique bool) *UsernameBlacklistQuery {
	ubq.ctx.Unique = &unique
	return ubq
}

// Order specifies how the records should be ordered.
func (ubq *UsernameBlacklistQuery) Order(o ...usernameblacklist.OrderOption) *UsernameBlacklistQuery {
	ubq.order = append(ubq.order, o...)
	return ubq
}

// First returns the first UsernameBlacklist entity from the query.
// Returns a *NotFoundError when no UsernameBlacklist was found.
func (ubq *UsernameBlacklistQuery) First(ctx context.Context) (*UsernameBlacklist, error) {
	nodes, err := ubq.Limit(1).All(setContextOp(ctx, ubq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{usernameblacklist.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (ubq *UsernameBlacklistQuery) FirstX(ctx context.Context) *UsernameBlacklist {
	node, err := ubq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first UsernameBlacklist ID from the query.
// Returns a *NotFoundError when no UsernameBlacklist ID was found.
func (ubq *UsernameBlacklistQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = ubq.Limit(1).IDs(setContextOp(ctx, ubq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{usernameblacklist.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (ubq *UsernameBlacklistQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := ubq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single UsernameBlacklist entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one UsernameBlacklist entity is found.
// Returns a *NotFoundError when no UsernameBlacklist entities are found.
func (ubq *UsernameBlacklistQuery) Only(ctx context.Context) (*UsernameBlacklist, error) {
	nodes, err := ubq.Limit(2).All(setContextOp(ctx, ubq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{usernameblacklist.Label}
	default:
		return nil, &NotSingularError{usernameblacklist.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (ubq *UsernameBlacklistQuery) OnlyX(ctx context.Context) *UsernameBlacklist {
	node, err := ubq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only UsernameBlacklist ID in the query.
// Returns a *NotSingularError when more than one UsernameBlacklist ID is found.
// Returns a *NotFoundError when no entities are found.
func (ubq *UsernameBlacklistQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = ubq.Limit(2).IDs(setContextOp(ctx, ubq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{usernameblacklist.Label}
	default:
		err = &NotSingularError{usernameblacklist.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (ubq *UsernameBlacklistQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := ubq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of UsernameBlacklists.
func (ubq *UsernameBlacklistQuery) All(ctx context.Context) ([]*UsernameBlacklist, error) {
	ctx = setContextOp(ctx, ubq.ctx, ent.OpQueryAll)
	if err := ubq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*UsernameBlacklist, *UsernameBlacklistQuery]()
	return withInterceptors[[]*UsernameBlacklist](ctx, ubq, qr, ubq.inters)
}

// AllX is like All, but panics if an error occurs.
func (ubq *UsernameBlacklistQuery) AllX(ctx context.Context) []*UsernameBlacklist {
	nodes, err := ubq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of UsernameBlacklist IDs.
func (ubq *UsernameBlacklistQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if ubq.ctx.Unique == nil && ubq.path != nil {
		ubq.Unique(true)
	}
	ctx = setContextOp(ctx, ubq.ctx, ent.OpQueryIDs)
	if err = ubq.Select(usernameblacklist.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (ubq *UsernameBlacklistQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := ubq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (ubq *UsernameBlacklistQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, ubq.ctx, ent.OpQueryCount)
	if err := ubq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, ubq, querierCount[*UsernameBlacklistQuery](), ubq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (ubq *UsernameBlacklistQuery) CountX(ctx context.Context) int {
	count, err := ubq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (ubq *UsernameBlacklistQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, ubq.ctx, ent.OpQueryExist)
	switch _, err := ubq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (ubq *UsernameBlacklistQuery) ExistX(ctx context.Context) bool {
	exist, err := ubq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the UsernameBlacklistQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (ubq *UsernameBlacklistQuery) Clone() *UsernameBlacklistQuery {
	if ubq == nil {
		return nil
	}
	return &UsernameBlacklistQuery{
		config:     ubq.config,
		ctx:        ubq.ctx.Clone(),
		order:      append([]usernameblacklist.OrderOption{}, ubq.order...),
		inters:     append([]Interceptor{}, ubq.inters...),
		predicates: append([]predicate.UsernameBlacklist{}, ubq.predicates...),
		// clone intermediate query.
		sql:  ubq.sql.Clone(),
		path: ubq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Username string `json:"username,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.UsernameBlacklist.Query().
//		GroupBy(usernameblacklist.FieldUsername).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (ubq *UsernameBlacklistQuery) GroupBy(field string, fields ...string) *UsernameBlacklistGroupBy {
	ubq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &UsernameBlacklistGroupBy{build: ubq}
	grbuild.flds = &ubq.ctx.Fields
	grbuild.label = usernameblacklist.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Username string `json:"username,omitempty"`
//	}
//
//	client.UsernameBlacklist.Query().
//		Select(usernameblacklist.FieldUsername).
//		Scan(ctx, &v)
func (ubq *UsernameBlacklistQuery) Select(fields ...string) *UsernameBlacklistSelect {
	ubq.ctx.Fields = append(ubq.ctx.Fields, fields...)
	sbuild := &UsernameBlacklistSelect{UsernameBlacklistQuery: ubq}
	sbuild.label = usernameblacklist.Label
	sbuild.flds, sbuild.scan = &ubq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a UsernameBlacklistSelect configured with the given aggregations.
func (ubq *UsernameBlacklistQuery) Aggregate(fns ...AggregateFunc) *UsernameBlacklistSelect {
	return ubq.Select().Aggregate(fns...)
}

func (ubq *UsernameBlacklistQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range ubq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, ubq); err != nil {
				return err
			}
		}
	}
	for _, f := range ubq.ctx.Fields {
		if !usernameblacklist.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if ubq.path != nil {
		prev, err := ubq.path(ctx)
		if err != nil {
			return err
		}
		ubq.sql = prev
	}
	return nil
}

func (ubq *UsernameBlacklistQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*UsernameBlacklist, error) {
	var (
		nodes = []*UsernameBlacklist{}
		_spec = ubq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*UsernameBlacklist).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &UsernameBlacklist{config: ubq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(ubq.modifiers) > 0 {
		_spec.Modifiers = ubq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, ubq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (ubq *UsernameBlacklistQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := ubq.querySpec()
	if len(ubq.modifiers) > 0 {
		_spec.Modifiers = ubq.modifiers
	}
	_spec.Node.Columns = ubq.ctx.Fields
	if len(ubq.ctx.Fields) > 0 {
		_spec.Unique = ubq.ctx.Unique != nil && *ubq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, ubq.driver, _spec)
}

func (ubq *UsernameBlacklistQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(usernameblacklist.Table, usernameblacklist.Columns, sqlgraph.NewFieldSpec(usernameblacklist.FieldID, field.TypeUUID))
	_spec.From = ubq.sql
	if unique := ubq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if ubq.path != nil {
		_spec.Unique = true
	}
	if fields := ubq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, usernameblacklist.FieldID)
		for i := range fields {
			if fields[i] != usernameblacklist.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := ubq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := ubq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := ubq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := ubq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (ubq *UsernameBlacklistQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(ubq.driver.Dialect())
	t1 := builder.Table(usernameblacklist.Table)
	columns := ubq.ctx.Fields
	if len(columns) == 0 {
		columns = usernameblacklist.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if ubq.sql != nil {
		selector = ubq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if ubq.ctx.Unique != nil && *ubq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range ubq.modifiers {
		m(selector)
	}
	for _, p := range ubq.predicates {
		p(selector)
	}
	for _, p := range ubq.order {
		p(selector)
	}
	if offset := ubq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := ubq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (ubq *UsernameBlacklistQuery) Modify(modifiers ...func(s *sql.Selector)) *UsernameBlacklistSelect {
	ubq.modifiers = append(ubq.modifiers, modifiers...)
	return ubq.Select()
}

// UsernameBlacklistGroupBy is the group-by builder for UsernameBlacklist entities.
type UsernameBlacklistGroupBy struct {
	selector
	build *UsernameBlacklistQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (ubgb *UsernameBlacklistGroupBy) Aggregate(fns ...AggregateFunc) *UsernameBlacklistGroupBy {
	ubgb.fns = append(ubgb.fns, fns...)
	return ubgb
}

// Scan applies the selector query and scans the result into the given value.
func (ubgb *UsernameBlacklistGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ubgb.build.ctx, ent.OpQueryGroupBy)
	if err := ubgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UsernameBlacklistQuery, *UsernameBlacklistGroupBy](ctx, ubgb.build, ubgb, ubgb.build.inters, v)
}

func (ubgb *UsernameBlacklistGroupBy) sqlScan(ctx context.Context, root *UsernameBlacklistQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(ubgb.fns))
	for _, fn := range ubgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*ubgb.flds)+len(ubgb.fns))
		for _, f := range *ubgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*ubgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ubgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// UsernameBlacklistSelect is the builder for selecting fields of UsernameBlacklist entities.
type UsernameBlacklistSelect struct {
	*UsernameBlacklistQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ubs *UsernameBlacklistSelect) Aggregate(fns ...AggregateFunc) *UsernameBlacklistSelect {
	ubs.fns = append(ubs.fns, fns...)
	return ubs
}

// Scan applies the selector query and scans the result into the given value.
func (ubs *UsernameBlacklistSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ubs.ctx, ent.OpQuerySelect)
	if err := ubs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UsernameBlacklistQuery, *UsernameBlacklistSelect](ctx, ubs.UsernameBlacklistQuery, ubs, ubs.inters, v)
}

func (ubs *UsernameBlacklistSelect) sqlScan(ctx context.Context, root *UsernameBlacklistQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ubs.fns))
	for _, fn := range ubs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ubs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ubs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (ubs *UsernameBlacklistSelect) Modify(modifiers ...func(s *sql.Selector)) *UsernameBlacklistSelect {
	ubs.modifiers = append(ubs.modifiers, modifiers...)
	return ubs
}
