// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"github.com/stablecog/sc-go/database/ent/predicate"
	"github.com/stablecog/sc-go/database/ent/thumbmarkidblacklist"
)

// ThumbmarkIdBlackListQuery is the builder for querying ThumbmarkIdBlackList entities.
type ThumbmarkIdBlackListQuery struct {
	config
	ctx        *QueryContext
	order      []thumbmarkidblacklist.OrderOption
	inters     []Interceptor
	predicates []predicate.ThumbmarkIdBlackList
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ThumbmarkIdBlackListQuery builder.
func (tiblq *ThumbmarkIdBlackListQuery) Where(ps ...predicate.ThumbmarkIdBlackList) *ThumbmarkIdBlackListQuery {
	tiblq.predicates = append(tiblq.predicates, ps...)
	return tiblq
}

// Limit the number of records to be returned by this query.
func (tiblq *ThumbmarkIdBlackListQuery) Limit(limit int) *ThumbmarkIdBlackListQuery {
	tiblq.ctx.Limit = &limit
	return tiblq
}

// Offset to start from.
func (tiblq *ThumbmarkIdBlackListQuery) Offset(offset int) *ThumbmarkIdBlackListQuery {
	tiblq.ctx.Offset = &offset
	return tiblq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (tiblq *ThumbmarkIdBlackListQuery) Unique(unique bool) *ThumbmarkIdBlackListQuery {
	tiblq.ctx.Unique = &unique
	return tiblq
}

// Order specifies how the records should be ordered.
func (tiblq *ThumbmarkIdBlackListQuery) Order(o ...thumbmarkidblacklist.OrderOption) *ThumbmarkIdBlackListQuery {
	tiblq.order = append(tiblq.order, o...)
	return tiblq
}

// First returns the first ThumbmarkIdBlackList entity from the query.
// Returns a *NotFoundError when no ThumbmarkIdBlackList was found.
func (tiblq *ThumbmarkIdBlackListQuery) First(ctx context.Context) (*ThumbmarkIdBlackList, error) {
	nodes, err := tiblq.Limit(1).All(setContextOp(ctx, tiblq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{thumbmarkidblacklist.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (tiblq *ThumbmarkIdBlackListQuery) FirstX(ctx context.Context) *ThumbmarkIdBlackList {
	node, err := tiblq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ThumbmarkIdBlackList ID from the query.
// Returns a *NotFoundError when no ThumbmarkIdBlackList ID was found.
func (tiblq *ThumbmarkIdBlackListQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = tiblq.Limit(1).IDs(setContextOp(ctx, tiblq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{thumbmarkidblacklist.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (tiblq *ThumbmarkIdBlackListQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := tiblq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ThumbmarkIdBlackList entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ThumbmarkIdBlackList entity is found.
// Returns a *NotFoundError when no ThumbmarkIdBlackList entities are found.
func (tiblq *ThumbmarkIdBlackListQuery) Only(ctx context.Context) (*ThumbmarkIdBlackList, error) {
	nodes, err := tiblq.Limit(2).All(setContextOp(ctx, tiblq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{thumbmarkidblacklist.Label}
	default:
		return nil, &NotSingularError{thumbmarkidblacklist.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (tiblq *ThumbmarkIdBlackListQuery) OnlyX(ctx context.Context) *ThumbmarkIdBlackList {
	node, err := tiblq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ThumbmarkIdBlackList ID in the query.
// Returns a *NotSingularError when more than one ThumbmarkIdBlackList ID is found.
// Returns a *NotFoundError when no entities are found.
func (tiblq *ThumbmarkIdBlackListQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = tiblq.Limit(2).IDs(setContextOp(ctx, tiblq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{thumbmarkidblacklist.Label}
	default:
		err = &NotSingularError{thumbmarkidblacklist.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (tiblq *ThumbmarkIdBlackListQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := tiblq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ThumbmarkIdBlackLists.
func (tiblq *ThumbmarkIdBlackListQuery) All(ctx context.Context) ([]*ThumbmarkIdBlackList, error) {
	ctx = setContextOp(ctx, tiblq.ctx, ent.OpQueryAll)
	if err := tiblq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ThumbmarkIdBlackList, *ThumbmarkIdBlackListQuery]()
	return withInterceptors[[]*ThumbmarkIdBlackList](ctx, tiblq, qr, tiblq.inters)
}

// AllX is like All, but panics if an error occurs.
func (tiblq *ThumbmarkIdBlackListQuery) AllX(ctx context.Context) []*ThumbmarkIdBlackList {
	nodes, err := tiblq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ThumbmarkIdBlackList IDs.
func (tiblq *ThumbmarkIdBlackListQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if tiblq.ctx.Unique == nil && tiblq.path != nil {
		tiblq.Unique(true)
	}
	ctx = setContextOp(ctx, tiblq.ctx, ent.OpQueryIDs)
	if err = tiblq.Select(thumbmarkidblacklist.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (tiblq *ThumbmarkIdBlackListQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := tiblq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (tiblq *ThumbmarkIdBlackListQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, tiblq.ctx, ent.OpQueryCount)
	if err := tiblq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, tiblq, querierCount[*ThumbmarkIdBlackListQuery](), tiblq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (tiblq *ThumbmarkIdBlackListQuery) CountX(ctx context.Context) int {
	count, err := tiblq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (tiblq *ThumbmarkIdBlackListQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, tiblq.ctx, ent.OpQueryExist)
	switch _, err := tiblq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (tiblq *ThumbmarkIdBlackListQuery) ExistX(ctx context.Context) bool {
	exist, err := tiblq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ThumbmarkIdBlackListQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (tiblq *ThumbmarkIdBlackListQuery) Clone() *ThumbmarkIdBlackListQuery {
	if tiblq == nil {
		return nil
	}
	return &ThumbmarkIdBlackListQuery{
		config:     tiblq.config,
		ctx:        tiblq.ctx.Clone(),
		order:      append([]thumbmarkidblacklist.OrderOption{}, tiblq.order...),
		inters:     append([]Interceptor{}, tiblq.inters...),
		predicates: append([]predicate.ThumbmarkIdBlackList{}, tiblq.predicates...),
		// clone intermediate query.
		sql:  tiblq.sql.Clone(),
		path: tiblq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		ThumbmarkID string `json:"thumbmark_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ThumbmarkIdBlackList.Query().
//		GroupBy(thumbmarkidblacklist.FieldThumbmarkID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (tiblq *ThumbmarkIdBlackListQuery) GroupBy(field string, fields ...string) *ThumbmarkIdBlackListGroupBy {
	tiblq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ThumbmarkIdBlackListGroupBy{build: tiblq}
	grbuild.flds = &tiblq.ctx.Fields
	grbuild.label = thumbmarkidblacklist.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		ThumbmarkID string `json:"thumbmark_id,omitempty"`
//	}
//
//	client.ThumbmarkIdBlackList.Query().
//		Select(thumbmarkidblacklist.FieldThumbmarkID).
//		Scan(ctx, &v)
func (tiblq *ThumbmarkIdBlackListQuery) Select(fields ...string) *ThumbmarkIdBlackListSelect {
	tiblq.ctx.Fields = append(tiblq.ctx.Fields, fields...)
	sbuild := &ThumbmarkIdBlackListSelect{ThumbmarkIdBlackListQuery: tiblq}
	sbuild.label = thumbmarkidblacklist.Label
	sbuild.flds, sbuild.scan = &tiblq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ThumbmarkIdBlackListSelect configured with the given aggregations.
func (tiblq *ThumbmarkIdBlackListQuery) Aggregate(fns ...AggregateFunc) *ThumbmarkIdBlackListSelect {
	return tiblq.Select().Aggregate(fns...)
}

func (tiblq *ThumbmarkIdBlackListQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range tiblq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, tiblq); err != nil {
				return err
			}
		}
	}
	for _, f := range tiblq.ctx.Fields {
		if !thumbmarkidblacklist.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if tiblq.path != nil {
		prev, err := tiblq.path(ctx)
		if err != nil {
			return err
		}
		tiblq.sql = prev
	}
	return nil
}

func (tiblq *ThumbmarkIdBlackListQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ThumbmarkIdBlackList, error) {
	var (
		nodes = []*ThumbmarkIdBlackList{}
		_spec = tiblq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ThumbmarkIdBlackList).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ThumbmarkIdBlackList{config: tiblq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(tiblq.modifiers) > 0 {
		_spec.Modifiers = tiblq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, tiblq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (tiblq *ThumbmarkIdBlackListQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := tiblq.querySpec()
	if len(tiblq.modifiers) > 0 {
		_spec.Modifiers = tiblq.modifiers
	}
	_spec.Node.Columns = tiblq.ctx.Fields
	if len(tiblq.ctx.Fields) > 0 {
		_spec.Unique = tiblq.ctx.Unique != nil && *tiblq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, tiblq.driver, _spec)
}

func (tiblq *ThumbmarkIdBlackListQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(thumbmarkidblacklist.Table, thumbmarkidblacklist.Columns, sqlgraph.NewFieldSpec(thumbmarkidblacklist.FieldID, field.TypeUUID))
	_spec.From = tiblq.sql
	if unique := tiblq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if tiblq.path != nil {
		_spec.Unique = true
	}
	if fields := tiblq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, thumbmarkidblacklist.FieldID)
		for i := range fields {
			if fields[i] != thumbmarkidblacklist.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := tiblq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := tiblq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := tiblq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := tiblq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (tiblq *ThumbmarkIdBlackListQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(tiblq.driver.Dialect())
	t1 := builder.Table(thumbmarkidblacklist.Table)
	columns := tiblq.ctx.Fields
	if len(columns) == 0 {
		columns = thumbmarkidblacklist.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if tiblq.sql != nil {
		selector = tiblq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if tiblq.ctx.Unique != nil && *tiblq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range tiblq.modifiers {
		m(selector)
	}
	for _, p := range tiblq.predicates {
		p(selector)
	}
	for _, p := range tiblq.order {
		p(selector)
	}
	if offset := tiblq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := tiblq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (tiblq *ThumbmarkIdBlackListQuery) Modify(modifiers ...func(s *sql.Selector)) *ThumbmarkIdBlackListSelect {
	tiblq.modifiers = append(tiblq.modifiers, modifiers...)
	return tiblq.Select()
}

// ThumbmarkIdBlackListGroupBy is the group-by builder for ThumbmarkIdBlackList entities.
type ThumbmarkIdBlackListGroupBy struct {
	selector
	build *ThumbmarkIdBlackListQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (tiblgb *ThumbmarkIdBlackListGroupBy) Aggregate(fns ...AggregateFunc) *ThumbmarkIdBlackListGroupBy {
	tiblgb.fns = append(tiblgb.fns, fns...)
	return tiblgb
}

// Scan applies the selector query and scans the result into the given value.
func (tiblgb *ThumbmarkIdBlackListGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, tiblgb.build.ctx, ent.OpQueryGroupBy)
	if err := tiblgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ThumbmarkIdBlackListQuery, *ThumbmarkIdBlackListGroupBy](ctx, tiblgb.build, tiblgb, tiblgb.build.inters, v)
}

func (tiblgb *ThumbmarkIdBlackListGroupBy) sqlScan(ctx context.Context, root *ThumbmarkIdBlackListQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(tiblgb.fns))
	for _, fn := range tiblgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*tiblgb.flds)+len(tiblgb.fns))
		for _, f := range *tiblgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*tiblgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := tiblgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ThumbmarkIdBlackListSelect is the builder for selecting fields of ThumbmarkIdBlackList entities.
type ThumbmarkIdBlackListSelect struct {
	*ThumbmarkIdBlackListQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (tibls *ThumbmarkIdBlackListSelect) Aggregate(fns ...AggregateFunc) *ThumbmarkIdBlackListSelect {
	tibls.fns = append(tibls.fns, fns...)
	return tibls
}

// Scan applies the selector query and scans the result into the given value.
func (tibls *ThumbmarkIdBlackListSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, tibls.ctx, ent.OpQuerySelect)
	if err := tibls.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ThumbmarkIdBlackListQuery, *ThumbmarkIdBlackListSelect](ctx, tibls.ThumbmarkIdBlackListQuery, tibls, tibls.inters, v)
}

func (tibls *ThumbmarkIdBlackListSelect) sqlScan(ctx context.Context, root *ThumbmarkIdBlackListQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(tibls.fns))
	for _, fn := range tibls.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*tibls.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := tibls.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (tibls *ThumbmarkIdBlackListSelect) Modify(modifiers ...func(s *sql.Selector)) *ThumbmarkIdBlackListSelect {
	tibls.modifiers = append(tibls.modifiers, modifiers...)
	return tibls
}
