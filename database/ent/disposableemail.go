// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/stablecog/sc-go/database/ent/disposableemail"
)

// DisposableEmail is the model entity for the DisposableEmail schema.
type DisposableEmail struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// Domain holds the value of the "domain" field.
	Domain string `json:"domain,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
}

// scanValues returns the types for scanning values from sql.Rows.
func (*DisposableEmail) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case disposableemail.FieldDomain:
			values[i] = new(sql.NullString)
		case disposableemail.FieldCreatedAt, disposableemail.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case disposableemail.FieldID:
			values[i] = new(uuid.UUID)
		default:
			return nil, fmt.Errorf("unexpected column %q for type DisposableEmail", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the DisposableEmail fields.
func (de *DisposableEmail) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case disposableemail.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				de.ID = *value
			}
		case disposableemail.FieldDomain:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field domain", values[i])
			} else if value.Valid {
				de.Domain = value.String
			}
		case disposableemail.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				de.CreatedAt = value.Time
			}
		case disposableemail.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				de.UpdatedAt = value.Time
			}
		}
	}
	return nil
}

// Update returns a builder for updating this DisposableEmail.
// Note that you need to call DisposableEmail.Unwrap() before calling this method if this DisposableEmail
// was returned from a transaction, and the transaction was committed or rolled back.
func (de *DisposableEmail) Update() *DisposableEmailUpdateOne {
	return NewDisposableEmailClient(de.config).UpdateOne(de)
}

// Unwrap unwraps the DisposableEmail entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (de *DisposableEmail) Unwrap() *DisposableEmail {
	_tx, ok := de.config.driver.(*txDriver)
	if !ok {
		panic("ent: DisposableEmail is not a transactional entity")
	}
	de.config.driver = _tx.drv
	return de
}

// String implements the fmt.Stringer.
func (de *DisposableEmail) String() string {
	var builder strings.Builder
	builder.WriteString("DisposableEmail(")
	builder.WriteString(fmt.Sprintf("id=%v, ", de.ID))
	builder.WriteString("domain=")
	builder.WriteString(de.Domain)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(de.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(de.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// DisposableEmails is a parsable slice of DisposableEmail.
type DisposableEmails []*DisposableEmail

func (de DisposableEmails) config(cfg config) {
	for _i := range de {
		de[_i].config = cfg
	}
}
