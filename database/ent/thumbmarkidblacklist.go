// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/stablecog/sc-go/database/ent/thumbmarkidblacklist"
)

// ThumbmarkIdBlackList is the model entity for the ThumbmarkIdBlackList schema.
type ThumbmarkIdBlackList struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// ThumbmarkID holds the value of the "thumbmark_id" field.
	ThumbmarkID string `json:"thumbmark_id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt    time.Time `json:"updated_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ThumbmarkIdBlackList) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case thumbmarkidblacklist.FieldThumbmarkID:
			values[i] = new(sql.NullString)
		case thumbmarkidblacklist.FieldCreatedAt, thumbmarkidblacklist.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case thumbmarkidblacklist.FieldID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ThumbmarkIdBlackList fields.
func (tibl *ThumbmarkIdBlackList) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case thumbmarkidblacklist.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				tibl.ID = *value
			}
		case thumbmarkidblacklist.FieldThumbmarkID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field thumbmark_id", values[i])
			} else if value.Valid {
				tibl.ThumbmarkID = value.String
			}
		case thumbmarkidblacklist.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				tibl.CreatedAt = value.Time
			}
		case thumbmarkidblacklist.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				tibl.UpdatedAt = value.Time
			}
		default:
			tibl.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the ThumbmarkIdBlackList.
// This includes values selected through modifiers, order, etc.
func (tibl *ThumbmarkIdBlackList) Value(name string) (ent.Value, error) {
	return tibl.selectValues.Get(name)
}

// Update returns a builder for updating this ThumbmarkIdBlackList.
// Note that you need to call ThumbmarkIdBlackList.Unwrap() before calling this method if this ThumbmarkIdBlackList
// was returned from a transaction, and the transaction was committed or rolled back.
func (tibl *ThumbmarkIdBlackList) Update() *ThumbmarkIdBlackListUpdateOne {
	return NewThumbmarkIdBlackListClient(tibl.config).UpdateOne(tibl)
}

// Unwrap unwraps the ThumbmarkIdBlackList entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (tibl *ThumbmarkIdBlackList) Unwrap() *ThumbmarkIdBlackList {
	_tx, ok := tibl.config.driver.(*txDriver)
	if !ok {
		panic("ent: ThumbmarkIdBlackList is not a transactional entity")
	}
	tibl.config.driver = _tx.drv
	return tibl
}

// String implements the fmt.Stringer.
func (tibl *ThumbmarkIdBlackList) String() string {
	var builder strings.Builder
	builder.WriteString("ThumbmarkIdBlackList(")
	builder.WriteString(fmt.Sprintf("id=%v, ", tibl.ID))
	builder.WriteString("thumbmark_id=")
	builder.WriteString(tibl.ThumbmarkID)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(tibl.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(tibl.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// ThumbmarkIdBlackLists is a parsable slice of ThumbmarkIdBlackList.
type ThumbmarkIdBlackLists []*ThumbmarkIdBlackList
